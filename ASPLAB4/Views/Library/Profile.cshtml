@model User

<h2>Профіль користувача</h2>

@if (Model != null)
{
    <div>
        <h3>Ім'я: @Model.Name</h3>
        <p>Email: @Model.Email</p>
        <p>ID: @Model.Id</p>
    </div>
}
else
{
    <p>Інформація про користувача недоступна.</p>
}
